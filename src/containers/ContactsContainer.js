import React, { Component } from 'react';

class Contacts extends Component {
  state = {
    contacts: [],
  }
  
  render() {
    return (
      <div>
        <h2>Contacts</h2>
      </div>
    );
  }
};

export default Contacts;
